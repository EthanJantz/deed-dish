{
  "name": "frontend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "serve-static": "^2.2.0",
    "wrangler": "^4.33.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "maplibre-gl": "^5.7.0",
    "pmtiles": "^4.3.0"
  },
  "scripts": {
    "build": "bun build index.html --outdir ./dist --target browser && cp about.html style.css ./dist/",
    "serve": "bun --hot index.ts",
    "dev": "bun run build && bun run serve"
  }
}
